/*

 */

// #include <bits/stdc++.h>
#include <algorithm>
#include <cstdio>
#include <cstdlib>
#include <cstring>
#include <iostream>
#include <vector>

// #define REDIRECT
using namespace std;

// typedef long long ll;

int main() {
#ifdef REDIRECT
    if (freopen("./p3.in", "r", stdin) == NULL) {
       fprintf(stderr,"Error: %s\n", strerror(errno));
    }
#endif

    int n;
    scanf("%d", &n);

    printf("%d\n", n);

#ifdef REDIRECT
    fclose(stdin);
#endif
    return 0;
}